int a[10];//initialize the array
typedef int* IntPtr;//create the pointer ... set?
IntPtr p;//create pointer p

p=a;//assign pointer to address of a



//Pointer Arithmetic?

typedef double* DoublePtr;
const int SIZE=10;
DoublePtr p;
d=new double[SIZE]

for(int i=0;i<SIZE;i++){
	cout<<*(d+i)<<" "; //apparently this is ugly...
	
}
